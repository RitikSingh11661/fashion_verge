import { Box, Text } from "@chakra-ui/react";
import React from "react";

const Logout = () => {
  return (
    <Box>
      <Text fontSize={"2xl"} fontWeight={"semibold"}>Logout</Text>
    </Box>
  );
};

export default Logout;